# Reference datasets are NOT redistributed here -- each is fetched by the user
# from its source. Run `python scripts/fetch_reference_data.py` for instructions.
#
# TEDesignLab states citation requirements but no redistribution licence, so the
# file is deliberately untracked and has been purged from this repository's
# history. Materials Project is CC-BY 4.0 but too large to track usefully.
data/reference/tedesignlab-complete-data.xlsx
data/reference/mp_190822.csv

# CSV exports are derived from the parquet tables and an order of magnitude
# larger. Regenerate with: python scripts/export_csv.py
data/annotated/csv/

__pycache__/
*.pyc
.venv/
.DS_Store

# unattended-run logs and raw stream-json output
run_chunks.*.log
run_chunks.*.jsonl
